export default function Home() {
  return (
    <main className="min-h-screen w-full">
      {/* 
        This is intentionally left empty.
        The cinematic background is handled by the CinematicBackground component in layout.tsx.
        Later sections can be placed here.
      */}
    </main>
  );
}
